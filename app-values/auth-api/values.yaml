replicaCount: 1

namespace: auth-api

persistence:
  enabled: true
  claimName: auth-api-pvc
  mountPath: /data

containers:
  - name: auth-api
    image: eclipse-temurin:21-jre
    port: 8080
    resources:
      requests:
        cpu: 100m
        memory: 128Mi
      limits:
        cpu: 300m
        memory: 256Mi
    volumeMounts:
      - name: app-storage
        mountPath: /app/data

volumes:
  - name: app-storage
    persistentVolumeClaim:
      claimName: auth-api-pvc
