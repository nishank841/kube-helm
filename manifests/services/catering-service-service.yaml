apiVersion: v1
kind: Service
metadata:
  name: catering-service-svc
  namespace: catering-service
spec:
  selector:
    app.kubernetes.io/name: app
    app.kubernetes.io/instance: catering-service
  ports:
  - port: 80
    targetPort: 80
  type: NodePort
