apiVersion: v1
kind: Service
metadata:
  name: auth-api-svc
  namespace: auth-api
spec:
  selector:
    app.kubernetes.io/name: app
    app.kubernetes.io/instance: auth-api
  ports:
  - port: 80
    targetPort: 80
    nodePort: 30081
  type: NodePort
